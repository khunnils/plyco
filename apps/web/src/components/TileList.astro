---
interface Props {
	items: string[];
}

const { items } = Astro.props;
---

<ul class="mt-6 grid gap-3 sm:grid-cols-2">
	{items.map((item) => <li class="rounded-lg border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-medium text-slate-700">{item}</li>)}
</ul>
