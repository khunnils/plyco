{
  "name": "@complyflow/api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@complyflow/db": "workspace:*",
    "@complyflow/shared": "workspace:*",
    "@fastify/cors": "^11.2.0",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.2",
    "nunjucks": "^3.2.4",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/nunjucks": "^3.2.6",
    "tsx": "^4.21.0",
    "vitest": "^4.0.16"
  }
}
