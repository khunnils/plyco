# Shared local defaults.
#
# App-specific files such as apps/api/.env and apps/client/.env can override
# these values where supported.

NODE_ENV=development
VITE_API_URL=http://localhost:4000

# Team invitation email sending.
RESEND_API_KEY=
INVITATION_EMAIL_FROM=Plyco <invites@example.com>
